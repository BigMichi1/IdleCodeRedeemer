#!/bin/sh

# Get the project root directory
PROJECT_ROOT="$(cd "$(dirname "$0")/.." && pwd)"

# Validate commit message format via local bin/mise
"$PROJECT_ROOT/bin/mise" run -- bunx commitlint --edit "$1"
