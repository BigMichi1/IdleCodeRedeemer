{
  "name": "idle-code-redeemer-bot",
  "version": "3.0.0",
  "description": "Discord bot for Idle Champions code scanning and redemption",
  "main": "dist/bot/bot.js",
  "docs": "docs",
  "scripts": {
    "typecheck": "tsc -p tsconfig.bot.json",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "build": "bun build --compile --bytecode --minify --sourcemap src/bot/bot.ts --outfile dist-bundle/bot",
    "start": "./dist-bundle/bot",
    "dev": "bun run --bun src/bot/bot.ts",
    "db:generate": "drizzle-kit generate",
    "db:studio": "drizzle-kit studio",
    "lint": "eslint src scripts --ext .ts --max-warnings 0",
    "lint:fix": "eslint src scripts --ext .ts --fix",
    "format": "syncpack fix && prettier --write \"src/**/*.{ts,md}\" \"scripts/**/*.ts\" \"docs/**/*.md\" \".github/**/*.{yml,yaml}\" \".mise.toml\" \"Dockerfile*\" \"*.json\"",
    "format:check": "syncpack lint && prettier --check \"src/**/*.{ts,md}\" \"scripts/**/*.ts\" \"docs/**/*.md\" \".github/**/*.{yml,yaml}\" \".mise.toml\" \"Dockerfile*\" \"*.json\"",
    "pre-commit": "lint-staged",
    "prepare": "husky"
  },
  "keywords": [
    "discord",
    "bot",
    "idle-champions"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "discord.js": "14.27.0",
    "drizzle-orm": "0.45.2",
    "winston": "3.19.0"
  },
  "devDependencies": {
    "@commitlint/config-conventional": "21.2.2",
    "@eslint/js": "10.0.1",
    "@tsconfig/bun": "1.0.11",
    "@types/bun": "1.4.0",
    "@typescript-eslint/eslint-plugin": "8.67.0",
    "@typescript-eslint/parser": "8.67.0",
    "commitlint": "21.2.2",
    "drizzle-kit": "0.31.10",
    "eslint": "10.9.0",
    "husky": "9.1.7",
    "lint-staged": "17.3.0",
    "prettier": "3.9.6",
    "prettier-plugin-sh": "0.19.0",
    "prettier-plugin-toml": "2.0.6",
    "syncpack": "15.3.3",
    "typescript": "6.0.3"
  },
  "peerDependencies": {
    "typescript": "6.0.3"
  },
  "lint-staged": {
    "*.ts": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{md,yml,yaml}": [
      "prettier --write"
    ],
    "package.json": [
      "sh -c 'syncpack fix' --",
      "prettier --write"
    ],
    "Dockerfile*": [
      "prettier --write"
    ],
    "*.json": [
      "prettier --write"
    ]
  }
}
